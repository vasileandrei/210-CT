'''This code return the highest perfect square of a number that is
less or equal to the number inputed.'''


def squareCheck(numberInputed):
    for number in range (0,numberInputed):
        if number*number == numberInputed:
            return (number*number)
        elif number*number > numberInputed:
            return (number-1) * (number-1)

#Pseudocode:
""" SQUARECHECK(numberInputed)
      for number <- 0 to numberInputed
        if number*number = numberInputed
          return (number*number)
        else if number*number > numberInputed:
          return (number-1) * (number-1)
"""
