"""Removes the vowels from a string of text"""

def vowelRemover(string):

    if string == "":
        return string
    elif string[0] in "aeiouAEIOU":
        return vowelRemover(string[1:])
    return string[0] + vowelRemover(string[1:])

#Pseudocode
"""
VOWELREMOVER(string):
    if string= "":
        return string
    else if string[0] in "aeiouAEIOU":
        return VOWELREMOVER(string[1:])
    return string[0] + VOWELREMOVER(string[1:])
"""
